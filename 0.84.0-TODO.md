# 0.84.0 stdlib adoption TODO

Scan date: 2026-09-05 (compiler v0.84.0/v0.84.1 API surface, vendored snapshot v0.84.0)
Pin status: `kukicha.org/kukicha/stdlib v0.84.0` — current

## Result: clean — nothing to do

No renamed/deprecated API usage and no hand-rolled equivalents of the v0.84.0 additions were found in any `.kuki` file (agent.kuki, bridge.kuki, config.kuki, local_tools.kuki, main.kuki).

## Verified clean (checked, no hand-rolled equivalents)

`cast.Smart*`, `validate.ParseBool`/`WithMessage`, `parse.WriteCSV*`, sandbox `.Write/.Append`, `set.Intersect/IsSubset/IsSuperset`, `MatchSafe` old arg order, old-order `ParallelWithLimit`, lowercase semver fields, `agentevent.Decode*` (no agent-log parsing — the "mini-claude-code mode" comment in local_tools.kuki is not an agentevent consumer); datetime midnight/`TimeAgo` math; `env.GetDuration`/`GetList`; base64/hex; crypto/uuid; sort.Slice closures; worker pools/WaitGroups; retry backoff; markdown; template re-parsing; `html.Attr`/`EscapeJoin`; `url.JoinPath`/`Hostname`/`Port`/`ParamOr`; signal; `parse.YAML`/`parse.Bool`/`strconv.ParseBool`; `input.ChooseValue`; `log.With`/`Set*`; `table.AddRowValues`; set/maps/iterator ops; `random.*`; `errors.Is`/`AsType` (config.kuki:160,210 wrap `env.GetOr` thinly — fine); regex named groups/`QuoteMeta`/`Count`; archive; git; `files.ReadStringOr`/`TempFileAuto`/`WatchCtx`; sandbox; netguard; shellguard; validate float family; `jsonschema.From`; term/color. `cli.ParseArgs(flagParser, os.Args)` at main.kuki:42 is already the current API. `local_tools.kuki:377` split-on-newline is domain logic, not a `cast.ToStringList` fit.

## Devops/MLOps-lens adoption

- [x] **Ctx alias cleanup** — `agent.kuki:8` imports `stdlib/ctx` as ctxpkg because the stdlib registry used to leak source import aliases into public signatures. Fixed in 0.84.1 (d18a32c1, canonicalize stdlib import aliases in generated signatures): ctx-taking stdlib calls accept a plain `import "stdlib/ctx"`. Dropped 2026-09-06.
- [ ] **Tool-output parsing** — `regex.FindNamed`/`FindNamedOr` (new) if the bridge/local-tools layer starts extracting structured fields from model or tool output by name (cleaner than index-based `FindGroups` remapping).
- [ ] **Bridge query params** — `url.ParamOr` (new) for optional query parameters in bridge HTTP handlers, defaults inline.
- [ ] **Run journal** — `sandbox.AppendJSON` (new) if local tool invocations should leave an append-only JSONL audit trail under a sandbox root (pairs with the "mini-claude-code mode" tooling in local_tools.kuki). Working shape to copy: gke-django-deploy's `RunEvent`/`recordRun`/`lastRun` — one JSONL line per event plus the same event overwriting a `status.json`, read back typed with `box.ReadJSON of T from "status.json" onerr return empty` as `optional reference T`; event struct fields must be exported to survive the round-trip.

## Execution

2026-06-09 correction: the ctx-alias fix actually shipped in v0.84.1 — the scan note predates the release cut and was wrong. Executed 2026-09-06: `kukicha init` re-run (pin v0.84.0 → v0.84.1), ctxpkg alias dropped in agent.kuki, fmt/check/build clean. Remaining Devops-lens items are conditional (no current trigger) — left unchecked.
